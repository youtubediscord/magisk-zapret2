# Эксплуатация Zapret2: поддержка, разрешения и восстановление

Этот документ дополняет основной README точными эксплуатационными границами APK и
Magisk-модуля. Он описывает контракт исходников. Подписанный релиз и конкретное
устройство считаются проверенными только после прохождения release/device-матрицы.

## Поддерживаемая конфигурация

| Компонент | Контракт |
| --- | --- |
| Android | API 24 (Android 7.0) или новее |
| Архитектура | ARM64 или ARM; x86/x86_64 не поставляются |
| Root-менеджер | Magisk 20.4+ |
| Firewall | IPv4 `iptables` с `NFQUEUE`, `--queue-bypass`, `connbytes`, `multiport` и `mark` |
| IPv6 | Требует того же набора в `ip6tables`; при его отсутствии IPv6 явно не перехватывается |
| Интерфейс | Jetpack Compose, Material 3 Expressive, светлая/тёмная тема и dynamic color на Android 12+ |

KernelSU, APatch и другие root-менеджеры пока не входят в подтверждённую матрицу.
Приложение должно показать неподдерживаемое или неполное окружение и не начинать
изменение firewall, если обязательный контракт не доказан.

## Зачем APK нужны разрешения

- `INTERNET` — только для запроса метаданных релиза и скачивания выбранных APK/ZIP.
- `ACCESS_NETWORK_STATE` — для отображения типа текущего подключения.
- `REQUEST_INSTALL_PACKAGES` — для передачи уже проверенного APK системному
  установщику. Приложение не может незаметно завершить установку за пользователя.
- Root выдаётся отдельно через Magisk/libsu. Он нужен для чтения защищённого состояния,
  изменения конфигурации, запуска/остановки сервиса и транзакционного обновления модуля.

Приватный `FileProvider` не экспортируется и выдаёт временный read-доступ только к
проверенному APK внутри `cache/updates/`. Доступ ко всему cache или внешнему хранилищу
не предоставляется.

## Что сохраняется при совместимом обновлении

После миграции единственным live-источником runtime-настроек является
`/data/adb/modules/zapret2/zapret2/runtime.ini`. Старые `config.sh` и
`/data/local/tmp/zapret2-user.conf` могут участвовать только в явной миграции и не
используются как скрытый fallback.

Совместимая установка или обновление сохраняет только разрешённые пользовательские
данные:

- `runtime.ini`, состояние категорий и безопасные bootstrap-настройки;
- пользовательские hostlist-файлы;
- точный bounded command-line файл, выбранный в `runtime.ini`;
- `zapret-custom.lua` и `init_vars.lua`, если это обычные не-symlink файлы размером
  не более 256 КиБ.

Поставляемые Lua-файлы, скрипты, бинарники и прочие release-owned файлы всегда берутся
из нового архива. Повреждённые, небезопасные или конфликтующие файлы не переносятся.
Downgrade через APK не поддерживается; модуль также отклоняет понижение версии.

## Нормальное восстановление

APK и модуль используют один сериализованный lifecycle/update-контракт. Актуальные
версии протоколов: owner v6 (33 поля), update lock v2, transaction v3 и cleanup v2.
После прерывания приложение сначала пытается доказать и завершить либо откатить
сохранённую транзакцию. Не удаляйте вручную `/data/adb/zapret2-state`, `update.lock`,
`update.transaction` или `update.cleanup`: это доказательства, необходимые recovery.

Безопасный порядок действий:

1. Откройте Control и дождитесь завершения автоматической проверки состояния.
2. Если доступно, повторите операцию или выполните обычную остановку/перезапуск.
3. Для подтверждённо повреждённой установки используйте Full rollback в Control.
4. Сохраните вывод Logs/Diagnostics; экспорт ограничен последними 64 КиБ и проходит
   редактирование приватных идентификаторов.
5. Если приложение не может доказать безопасное состояние, перезагрузитесь и повторите
   проверку до любых ручных удалений.

Fail-closed сообщение не означает, что нужно стирать state-файлы. Оно означает, что
автоматическая операция отказалась продолжать без достаточного доказательства.

## Удаление и чистая переустановка

1. Остановите сервис в Control, если операция доступна.
2. Удалите модуль через доверенный интерфейс Magisk, а не через файловый менеджер.
3. Перезагрузите устройство.
4. APK можно удалить обычным системным способом.
5. Для чистой переустановки установите модуль заново только после завершения удаления
   и перезагрузки.

Uninstall сначала публикует защищённый tombstone, затем выполняет bounded stop и
проверяет отсутствие принадлежащих Zapret2 процессов/firewall-правил. Неавторизованные
или неоднозначные файлы намеренно сохраняются для диагностики вместо слепого удаления.

## Диагностика

Базовые root-команды:

```sh
su -c zapret2-status
su -c zapret2-stop
su -c zapret2-start
su -c zapret2-restart
```

Проверьте в таком порядке:

1. Magisk действительно выдал APK root и модуль не отключён/не ожидает удаления.
2. Control не показывает active update/recovery или partial package state.
3. В статусе доказаны текущий owner, PID/starttime, generation и ruleset.
4. Ядро поддерживает обязательные NFQUEUE/firewall-возможности.
5. AdGuard, NetGuard, AFWall+ и другие владельцы firewall временно отключены для
   изолированной проверки.
6. Для стратегии выбран совместимый preset/category и существующие hostlist-файлы.

Read-only device preflight и формат сохраняемых доказательств описаны в
[`tests/device/README.md`](../tests/device/README.md). Изменяющие стадии допускаются
только на одноразовом устройстве с явными подтверждениями, указанными в harness.

## Известные границы текущего дерева

- Material 3 Expressive закреплён на экспериментальном API; это не stable Material 3.
- Source-аудит не заменяет compile/lint/test, screenshot/TalkBack и rooted-device gates.
- KernelSU/APatch, x86 и устройства без обязательного NFQUEUE-контракта не заявлены как
  поддержанные.
- До публикации следует сверить SHA-256 и подпись именно финальных APK/ZIP, а не только
  имена файлов или страницу релиза.
