## Legacy Bootstrap Final Cleanup

- [completed] Remove the remaining live bootstrap fallback. Shell and Android now require one complete runtime-v1 `[core]`; missing/partial state self-heals from built-in values without reading `config.sh` or `/data/local/tmp/zapret2-user.conf`, while the explicit migration script remains the only bootstrap consumer.
- [completed] `categories.ini` remains explicit category-state and should not absorb runtime config.
- [in progress] Run on-device verification for restart, reboot, reinstall, and app/module update flows.
- [in progress] Source contracts and regression fixtures now cover missing/partial `runtime.ini`, preservation of valid partial core values and non-core sections, duplicate/malformed rejection, and proof that self-heal does not revive legacy bootstrap values. Rooted-device restart/reboot confirmation remains pending.
- [completed] Final source sweep removed live `legacy-fallback`/`bootstrap-fallback` branches and unused bootstrap-path metadata from the authoritative runtime file; compatibility parsers remain only at the explicit migration/recovery boundaries.

## Release Supply-Chain and Preservation Closure

- [completed] Pin every third-party GitHub Action to an immutable commit and scope `contents: write` to the release job.
- [completed] Resolve the latest stable zapret2 release at build time, verify its GitHub-published asset digest and upstream Android binary checksums, and record the resolved commit in the package.
- [completed] Keep release signing fail-closed; verify the APK with `apksigner`, assert the expected signer-certificate SHA-256 secret, and publish verified APK/ZIP SHA-256 sidecars.
- [completed] Preserve only `zapret-custom.lua` and `init_vars.lua` as bounded regular non-symlink user Lua extensions across both installer and hot-update paths; preserve the exact safe bounded command-line file bound by `runtime.ini`; never restore core/package Lua or overwrite release-owned files from an old install.
- [completed] Extend source/unit/shell contracts for the Lua allowlist, core-Lua exclusion, size/symlink rejection, package identity, wrappers, ABI, and recovery entry points.
- [completed] Enable `lockAllConfigurations()`, generate and review `app/gradle.lockfile` (214 modules across 54 resolved configurations) and least-privilege strict SHA-256 `gradle/verification-metadata.xml` (414 components/723 artifacts; SHA-256 `42908BCD37C8AB4483FED38F0788C8829FCD667C753CE44CADF04857C8E774C0`), enforce strict verification in every Android CI Gradle invocation, and retain a static fail-closed policy test plus documented trusted-regeneration procedure.
- [ ] Run the release-resolving workflow with `KEYSTORE_BASE64`, `KEYSTORE_PASSWORD`, `KEY_ALIAS`, `KEY_PASSWORD`, and `APK_SIGNING_CERT_SHA256`, then retain the CI provenance and exact artifact hashes.
- [ ] Install/upgrade the exact signed CI artifacts on supported rooted devices and verify preservation, rollback, NFQUEUE lifecycle, reboot, and uninstall behavior.

### Rooted-device release evidence checklist

- [completed] Add an opt-in `tests/device/smoke.sh` harness whose default and dry-run paths are device-read-only, bind every run to one exact authorized ADB serial, capture a private before-state bundle, and fail every mutating stage unless disposable-device and tested-recovery acknowledgements are exact.
- [completed] Add deny-by-default fake-ADB host regressions proving that default preflight/dry-run do not mutate; missing serial/consent, denied root, and incomplete artifact identity fail before mutation; exact root-private update staging closes shell-temp TOCTOU; strict capability/status/rollback/stage schemas, every modeled staging and cleanup predicate, both install hash swaps, capability drift, mandatory/dirty firewall dumps, canonical reboot proof, exact dead-owner uninstall tombstones, and the complete IPv4-only uninstall flow fail closed as required.
- [ ] Run read-only preflight on each supported disposable device against the exact signed CI ZIP/APK hashes; retain API/ABI/root-manager, path/mode, NFQUEUE/queue-bypass, connbytes, multiport, mark, iptables/ip6tables, and strict machine-status evidence.
- [ ] Using one evidence directory per device/artifact pair, run and retain the ordered stop/start/restart/update/full-rollback stages; verify both idempotent lifecycle repetitions, exact installed hashes, the strict rollback contract, preservation, reboot behavior, and no orphan process/firewall state.
- [ ] Remove Zapret2 last through the trusted root-manager UI, reboot, run `uninstall-verify`, then test reinstall as a separate fresh evidence sequence. Do not mark this or the broader rooted-device items complete until real-device artifacts and results are retained.

## Lifecycle Cleanup Audit

**Architecture implementation status: COMPLETED.** Device and signed-release validation remains tracked separately below and does not reopen the implementation audit.

- [completed] Boot-bind PID/owner publication and recover stale different-boot metadata only after serialized exact process/firewall absence proof.
- [completed] Convert owned-firewall absence and cleanup predicates to captured-snapshot tri-state semantics; propagate every query failure.
- [completed] Journal every committed-generation anchor/rule/chain teardown operation durably, authenticate the exact owner-derived manifest, and rename chains to unique tombstones before X.
- [x] Replace legacy fixed-chain numeric teardown with schema-v6 generation-bound main chains and deterministic per-rule subchains; build, health, snapshot publication, and teardown now prove each unique jump -> exact one-rule subchain relationship, while v5 remains read-only legacy evidence.
- [completed] Make full-rollback transaction phases monotonic and resume double-interrupted hosts backup publication.
- [completed] Make recovery wildcard enumeration expand every artifact class under caller `set -f` without leaking shell-option changes.
- [completed] Require successful per-family `mangle -S` baselines before different-boot journal retirement.
- [completed] Bind detached-build journals to creator PID/starttime and boot ID; recover strictly validated stale journals only after a different-boot clean-ownership proof.
- [completed] Put NFQUEUE capability probes under durable write-ahead journaling from chain/rule creation through exact rule/chain deletion, with interruption regressions.

- [completed] Make command wrappers static package assets and close installer races with hot-update markers and uninstall-tombstone rollback.
- [completed] Replaced terminal-command symlinks with fixed installed-path wrappers; staged installs now retain durable rollback data, uninstall publishes a persistent authenticated tombstone before stop/unlock, and boot delegates update/removal refusal to the authoritative start command.
- [completed] Harden start/stop lifecycle cleanup: roll back NFQUEUE after failed starts; remove bypass-less and stale-QNUM rules; serialize lifecycle/boot triggers; rotate `nfqws2.log`; restore changed sysctl/permissions; and purge runtime diagnostics during stop/uninstall.
- [completed] Implement `/data/adb/zapret2-state` as the root-only lifecycle contract and keep process metadata, diagnostics, status, and lifecycle/update serialization out of public temporary storage.
- [completed] Keep `service.sh` as the sole boot owner (the manifest has no boot receiver), rotate `nfqws2.log` at 1 MiB with one retained generation, and report the real start exit code.
- [completed] During uninstall, retry stop within fixed bounds under one lifecycle lock, require a clean process/firewall audit, preserve active locks/restorable updates, remove the exact state directory only when empty, and remove legacy files only with exact ownership evidence.
- [completed] Keep install-time permission changes scoped to the exact module path, establish the root-only state directory through the shared helper, and preserve user runtime/category/bootstrap configuration, hostlists, and the bounded approved user-Lua allowlist across module updates.
- [ ] Verify the state-directory mode/owner, boot rotation, concurrent update/uninstall refusal, interrupted-update recovery, and legacy cleanup on supported rooted devices.

### Sweep P1 packaging/install/rollback closure

- [completed] Bind Android owner-state, firewall-topology verification, and package manifests to the exact 33-field v6 owner protocol. Android verifies current boot, generation-bound tag/chains, canonical payload/spec/rule cardinality, and the SHA-256 firewall fingerprint; a source policy test locks Android constants to shell versions and field order.
- [completed] Bind Android's independent live iptables proof to every owner-v6 payload field, not only tag/chains/count/qnum: protocol, direction, exact port/range groups, packet bounds, connbytes direction/mode, multiport form, mark value/mask/negation, NFQUEUE target and queue-bypass must all match the authenticated owner contract for each ordinal subchain.
- [completed] Close the final Android/shell scalar parity gap: current owner-v6 numeric fields are canonical on both sides and `desync_mark` is normalized and bounded to the unsigned 32-bit netfilter range in runtime and owner validation.
- [completed] Android owner-binding follow-up: cancellation-safe live-lock handoff, owner-token/digest CAS for every transaction phase and terminal cleanup, and a durable pre-mutation intent protocol for the active-to-backup swap hard-kill window.
- [completed] Make Android terminal-commit success bookkeeping cancellation-proof and verify cancellation during/immediately after the terminal shell command cannot enter rollback.
- [completed] Make Android terminal journal deletion the final fallible commit point, with pre-delete cleanup preflight and post-delete best-effort committed cleanup semantics shared by update and recovery.
- [completed] Preserve ambiguous terminal outcomes for recovery instead of rollback, and consume matching pre-commit cleanup evidence before restored-journal publication.
- [completed] Close the final Android update/recovery TOCTOU gaps: the fresh four-path workspace is checked under the exact owner before journal publication and candidate preparation never blindly deletes a pre-existing path; active-to-backup and candidate-to-active moves are owner/digest-CAS bound and repeat full source/destination tree plus disable-marker integrity immediately around the move; recovery rejects symlink, non-root, and unexpected-mode directories before classification; lifecycle scripts and terminal commit revalidate the exact root-owned active tree including sourced helpers; owner rebind publishes only the precomputed exact digest; hot rollback and retained-backup recovery repeat the live owner/journal guard plus active/source/destination integrity immediately before every stop, verified-stopped state, copy, move, and promotion boundary. Incomplete rollback now reports exact safe-backup and exact journal-digest evidence and truthfully states that the live owner lock is released for a fresh recovery owner instead of claiming it remains held.
- [completed] Android P1 follow-up: persist and recover exact disable-marker expectation, harden privileged mutation/recovery probes, widen process start ticks to canonical nonnegative 64-bit values, and enforce the runtime manifest as the exact package allowlist.
- [completed] Preserve an exact safe `disable` marker during standard install/update while rejecting unsafe marker types.
- [completed] Move mutable install snapshots under lifecycle/update serialization and audit shared recovery artifacts both before mutation and at commit.
- [completed] Make uninstall fail closed on partial rollback and retire only a completed rollback generation/hosts backup authenticated to the installed generation/archive.
- [completed] Clear only an authenticated stale rollback generation during install/reinstall.
- [completed] Require, chmod, package, and CI/test the executable `zapret-update-guard.sh` contract.
- [completed] Add regression sources for rollback -> uninstall -> reinstall, disabled install/update, recovery-artifact truth tables, cleanup recovery, CAS drift, cancellation, release failure, and idempotence. Execution gates remain pending per the source-only instruction.

## Full Material 3 Expressive Migration and Reliability Roadmap

The source migration and lifecycle implementation are complete. Release completion still requires the deliberately deferred compile/lint/test, visual-accessibility, signed-artifact, and rooted-device gates below; source completion must not be presented as release evidence.

### Phase 0 — Baseline, inventory, and requirements

- [ ] Record the current debug/release build status, unit/instrumentation test results, lint findings, APK size, startup behavior, and known device-specific failures before changing behavior.
- [completed] Inventory every current Compose destination, state owner, shared component/resource family and preview; record the source replacement/retention boundary in `docs/MD3E_SOURCE_COMPLETION_AUDIT.md`.
- [completed] Map the complete app-to-module flow from root/module discovery through configuration, lifecycle, logging, update/recovery, boot and uninstall in `docs/MD3E_SOURCE_COMPLETION_AUDIT.md`.
- [completed] Define the claimed API/ABI/Magisk/firewall/UI-mode source contract and explicitly keep KernelSU/APatch outside the verified release matrix until device evidence exists.
- [completed] Convert the source defects found during inventory into the named architecture, screen, update, recovery, cleanup and legacy-removal work items below.

Acceptance criteria:

- [completed] A reviewer can trace every existing screen and every privileged module operation to its owner, state model, boundary and verification source through `docs/MD3E_SOURCE_COMPLETION_AUDIT.md`.
- [ ] Baseline evidence is saved in the repository or linked from this file so regressions can be measured rather than guessed.

### Phase 1 — Architecture and state ownership

- [completed] Establish a single source of truth for UI state per feature (immutable state plus explicit events/effects); remove duplicated local state and shell-derived state that can drift.
- [completed] Move root, filesystem, process, config, strategy, hostlist, log, update, and lifecycle work behind typed repositories/use cases with injectable dispatchers and command execution.
- [completed] Model loading, ready, empty, permission-required, unsupported, recoverable-error, and fatal-error states explicitly; prevent stale success UI after a failed command.
- [completed] Make navigation and one-shot effects lifecycle-safe so rotation, process recreation, repeated taps, and back navigation do not duplicate commands or lose results.
- [completed] Define typed results and actionable error messages for every privileged operation; redact secrets and unsafe command content from user-visible logs.
- [completed] Add dependency boundaries that let app logic be tested without root or an installed module.

Acceptance criteria:

- [completed] No composable directly performs blocking filesystem or root-shell work.
- [completed] Each operation has one authoritative state owner and deterministic success, retry, cancel, and failure transitions.
- [completed] Direct unit-test sources cover state/process restoration, rapid duplicate/busy actions, cancellation at privileged lock/commit boundaries, root denied/missing/timeout, missing/partial module state, and malformed/duplicate/reordered/truncated machine output. The exact file mapping is retained in `docs/MD3E_EXPERIMENTAL_AND_TESTING.md`; execution remains part of the deferred synchronized Gradle gate.

### Phase 2 — Material 3 Expressive design system

- [completed] Pin a mutually compatible stable host toolchain (AGP 9.1.1, Gradle 9.3.1, JDK 17 and Kotlin 2.3.21) while explicitly isolating the required Material 3 Expressive surface on reviewed `material3:1.5.0-alpha24`; document every experimental opt-in and its API-by-API removal path. A fully stable Material 3 dependency cannot honestly satisfy the current Expressive API requirement yet.
- [completed] Define semantic color roles for light, dark, dynamic, success, warning, error, stopped, starting, and running states; remove hard-coded screen colors. No separate AMOLED mode is retained.
- [completed] Define expressive typography, shape, spacing, elevation, iconography, motion, and component-size tokens with accessible defaults.
- [completed] Build reusable app primitives for top bars, navigation, hero/status surfaces, buttons, toggles, cards, list rows, chips, segmented controls, banners, dialogs, sheets, empty/error/loading states, and destructive confirmations.
- [completed] Apply expressive motion consistently for state and navigation transitions while honoring the reduced-motion/system-animator policy.
- [completed] Ensure system bars, edge-to-edge layout, predictive back, IME/insets, gesture navigation, and screen cutouts use the theme correctly at source level.

Acceptance criteria:

- [completed] Every production destination and shared component consumes semantic design tokens/components; static source review found no ad-hoc screen/component colors, shapes, typography dimensions, or raw `dp` literals. Rendered visual acceptance remains a separate pending gate below.
- [completed] Shared section titles, the active screen title, navigation group labels, and the medium overflow title expose semantic heading roles; `AccessibilitySemanticsPolicyTest` guards the source contract.
- [completed] Lock the complete English/Russian localization contract: all 483 translatable strings and 14 plurals have key and format-argument parity, multi-argument formats use explicit positions, and every Russian plural defines `one/few/many/other` without mismatched argument types. The source call-site sweep also confirms all 37 references to the 34 formatted strings and all 17 plural calls supply the required arguments and a distinct plural quantity.
- [completed] Remove English-only `UiText.Dynamic` copy from loading/error/disabled destination previews and bind those states to the same resources as runtime ViewModels. Category subtitles now retain technical protocol/file data while localizing the app-owned no-filter state instead of displaying raw `none`. Static policy rejects hardcoded `Text`, semantic labels, and literal `UiText.Dynamic` copy from reachable Compose UI while allowing true runtime/user data.
- [in progress] All 36 fixed light/dark semantic foreground/background pairs exceed the WCAG 4.5:1 normal-text threshold at source level and are locked by `ThemeContrastPolicyTest` (current minimum 5.45:1). Dynamic color, composited/disabled content, controls, and rendered device states still require the visual-accessibility gate.
- [in progress] Source layouts include 360/600/840 dp, RTL, Russian and 200% font-scale previews; the app shell now has an explicit compact drawer below 600 dp, a seven-item accessible medium rail (six frequent destinations plus localized overflow sheet) at 600–839 dp, and an expanded grouped wide rail from 840 dp. Hidden medium destinations remain selected through the overflow item, and resizing closes any now-invisible drawer and invalidates stale exit prompts. High-risk Config/Hosts/DNS/Presets action groups, the Hostlist editor footer, and Control readiness badges stack below the reviewed width breakpoint or whenever font scale is at least 1.3. The preset editor stacks its actions, consumes IME insets and uses line-based growth without a fixed 200 dp minimum; primary card/list labels may wrap to two lines, Logs tabs scroll instead of compressing long labels, trailing setting values cannot displace their labels, and custom controls guarantee 48 dp targets. Rendered clipping, display scaling, keyboard navigation and TalkBack order still require the visual-accessibility gate.

### Phase 3 — Screen-by-screen expressive rebuild

- [completed] Rebuild the app shell and navigation with clear destination hierarchy, selected states, status presentation, and adaptive phone/tablet layouts. `NavigationDestinationPolicyTest` locks nine unique navigation destinations plus the single typed hostlist detail route to exactly ten NavHost registrations and the Control start destination.
- [completed] Rebuild Control/Home around one unambiguous module/service status, guarded primary actions, progress, last result, and recovery guidance.
- [completed] Rebuild Strategies with searchable/filterable categories, clear active/applied/edited states, validation, safe apply/reset flows, and conflict visibility.
- [completed] Rebuild Hostlists with loading/empty/error states, scalable viewing/editing, validation, import/update feedback, and protection from accidental data loss.
- [completed] Rebuild Logs/Diagnostics with bounded rendering and sharing, refresh behavior, redaction, and useful empty/error states.
- [completed] Rebuild Settings/Configuration with validated inputs, explanatory summaries, defaults/reset, unsaved-change handling, and disabled-state explanations.
- [completed] Rebuild About/Update/Install surfaces with authoritative versions, compatibility state, changelog/progress/errors, and explicit destructive confirmations.
- [completed] Add previews for every destination through its real production entry point, covering normal plus applicable loading, empty, error, disabled, degraded, root-denied, and update-check transient states.
- [completed] Close busy-state interaction races in DNS selection, strategy reordering, and hostlist import/refresh: controls now expose disabled semantics and ViewModels independently reject stale/accessibility/programmatic actions. Shared loading surfaces expose indeterminate progress semantics and clickable setting rows declare a button role.
- [completed] Close Control setting races: autostart and packet mutations share an atomic busy gate, conflict with lifecycle/update/rollback actions at both UI and ViewModel boundaries, and clear their busy UI state on success, failure, blocking, exception and cancellation.
- [completed] Put every admitted Control runtime setting mutation behind one coordinator-owned cancellation fence: autostart and both packet limits now share the same transaction boundary, and startup normalization of deprecated `wifi_only=1` cannot be cancelled between write and verification.
- [completed] Close ambiguous preset exception paths: candidate write/validation failures clean the temporary file, replace and post-replace reread failures restore the exact previous target, and config/restart failures attempt config and file restoration independently before publishing a typed outcome.
- [completed] Make Config Editor post-write verification exception-safe: thrown cmdline writes, snapshots, validators, or restores are converted into explicit transaction outcomes, and a missing/failed post-state proof restores the captured command-line snapshot before UI publication.
- [completed] Make Strategies compensation fail closed: exceptions while re-reading or restoring categories/runtime mode no longer escape into an ordinary failure after mutation; cancellation still propagates, while uncertain compensation is reported as rollback failure.
- [completed] Remove broad `runCatching` from protected cmdline/hostlist/hosts mutation and rollback adapters. These paths now rethrow cancellation explicitly, map ordinary I/O failures to typed outcomes, and retain post-write reread plus exact restore ordering.
- [completed] Preserve cancellation across document-provider import, hostlist counting, and protected log read/clear adapters; ordinary provider/root I/O still maps to bounded typed failures, while cancellation reaches the owning ViewModel unchanged.
- [completed] Preserve cancellation through DNS runtime commits and module staging extraction; compensation/temporary-directory cleanup still runs before cancellation leaves the operation.
- [completed] Close lifecycle post-state cancellation gaps: once start/stop/restart or full rollback reaches its shell command, command execution and authoritative status verification finish under `NonCancellable`; a cancelled caller is resumed only after that verified outcome exists.
- [completed] Move protected multi-file log clearing to the shared non-cancellable mutation fence, restore caller cancellation after the command outcome exists, and re-verify each remaining log path's exact secure file contract after truncation/chmod.
- [completed] Revalidate Control runtime settings whenever the destination returns to STARTED, reject lifecycle/settings/update actions while any modal owns interaction, require exact dialog identity for packet/update confirmation, and skip no-op autostart/packet writes so stale/programmatic calls cannot mutate or restart under a different dialog.
- [completed] Replace check-then-act coordination between Control toggle, setting writes, update checks, update installation, and full rollback with one synchronously claimed exclusive action gate. Individual UI flags remain presentation state, while the shared atomic claim closes cross-operation races before any coroutine or privileged mutation starts.
- [completed] Bind each Control update-check result to the exact dialog state at launch: a manual check publishes only while no modal has appeared, and restored-update revalidation publishes only while that same update dialog remains open, so a late response cannot resurrect a dismissed modal or replace another flow.
- [completed] Make editor and catalog state machines independent of UI-only disabling: Config/Hosts reject draft mutation during operations; HostlistContent synchronously claims editor loads/saves and isolates search/paging; Strategies captures the desired configuration before asynchronous persistence and shares one gate with order writes; DNS/Hostlists convert unexpected read/import exceptions into terminal error states.
- [completed] Enforce Strategies filter-switch capability inside the ViewModel rather than only hiding its selector, and reject already-active strategy/filter, packet-count, and debug-mode selections before optimistic state or privileged persistence so repeated taps/programmatic calls cannot cause no-op restarts.
- [completed] Bind every strategy selection to the exact authoritative picker catalog loaded for that category; a merely well-formed but absent/stale strategy ID is rejected before categories.ini can be written or rollback can become necessary.
- [completed] Make Config Editor reject an ordinary clean save before mutation and reject `forceCmdline` unless restart is also requested. The plain Save control now reflects dirty state independently while the explicit restart action remains available for a clean authoritative command.
- [completed] Preserve preset edits across recreation within a bounded state budget, confirm dirty dismissal, keep the editor open after rejected/failed/rolled-back saves, and close it only after a confirmed saved outcome. Config/Hosts reload and Preset/Hostlist editor exit require explicit dirty-draft acknowledgement inside their ViewModels; DNS reset, hosts-overlay reset, and log clearing additionally require explicit action confirmation, so accessibility or programmatic callers cannot bypass the UI dialogs. A second preset-editor open cannot replace existing editor state, and clean hosts saves are rejected before mutation. Logs expose clear-in-progress state, block conflicting tab/refresh/actions at both UI and ViewModel boundaries, refuse new protected reads/actions while the screen is stopped, and generation-invalidate canceled reads before return.
- [completed] Reject already-active preset application and category-mode switching inside `PresetsViewModel`, not only through disabled Compose controls, so repeated/programmatic actions cannot rewrite runtime state or restart the service without a state change.
- [completed] Complete the source accessibility semantics sweep: every custom clickable/toggleable/selectable surface has an explicit role and an explicit 48 dp minimum target, radio-button collections expose `selectableGroup`, parent-owned checkbox/switch/radio rows clear duplicate child semantics, grouped status/readiness surfaces merge their text and state once, strategy selection/position state belongs to its named target, preset cards expose edit/apply exactly once, icon-only Material controls retain accessible names across visual/loading states, Material minimum-target enforcement is never disabled, and the blocking overlay exposes one polite indeterminate progress region; `AccessibilitySemanticsPolicyTest` guards these invariants. Hands-on TalkBack, focus-order, contrast, scaling, and touch-target verification remains in the external accessibility gate.

Acceptance criteria:

- [completed] No destination retains a legacy visual island or skips the common state/error/accessibility system in production source.
- [completed] Primary tasks—check status, start/stop, choose/apply strategy, inspect hostlists, read/export logs, and update—have explicit visible controls without hidden gestures.
- [ ] Screens remain usable at the smallest supported phone size, tablet width, landscape, 200% font scale, and with long Russian/English strings.

### Phase 4 — APK logic hardening

- [completed] Make root capability checks explicit, cancellable, and retryable; distinguish denial, unavailable root manager, shell failure, timeout, and unsupported environment. The source model now carries exact root acquisition states through lifecycle results, `ControlUiState`, and localized status rendering.
- [completed] Make module discovery/version/compatibility checks robust to missing, disabled, removal-pending, updating/recovery, partially installed, unreadable, and unsupported-ABI states. Control now re-probes the exact state, validates bounded `module.prop`, required runtime assets and executability, clears stale version/capability UI, and blocks unsafe operations while retaining stop cleanup for degraded ownership.
- [completed] Parse configuration, categories, strategies, status, and command output with strict validation and forward-compatible handling of validated unknown fields where the file contract permits extension.
- [completed] Make writes atomic (temporary file, validation, permissions/ownership, replace, rollback) and prevent concurrent operations from corrupting config or UI state. Editable hostlist/runtime/DNS/hosts/category/preset paths now share mutation serialization and explicit rollback-failed outcomes.
- [completed] Serialize start/stop/restart/apply/update operations and debounce repeated taps; surface the final verified module state instead of assuming command success.
- [completed] Close the final Control update tap window by claiming the global update lease and visible busy state synchronously before launching asynchronous work; release remains in the cancellation-safe terminal path and a source policy regression locks the ordering.
- [completed] Harden update download/verification/install flows for offline errors, bounded HTTPS redirects and release metadata, partial files, immutable URL-to-SHA-256 artifact binding, strict version comparison, APK package/signing-lineage/version mismatch, module ABI/package incompatibility, transactional rollback, and authenticated cleanup. Release metadata does not auto-follow redirects; initial assets must belong to this repository's exact GitHub release path and every explicit redirect is restricted to the GitHub release/CDN allowlist. Arbitrary HTTPS hosts, other repositories, user-info, non-443 ports, fragments and control characters fail before connection. APK compatibility distinguishes current signers from authenticated history, permits only the same current signer or forward rotation containing the installed current signer, and rejects an old historical signer as an update identity. APK `versionName`/`versionCode` and strict Magisk `module.prop` version/versionCode metadata are bound to the selected project release; the module update channel cannot drift, and the retired nonexistent `webRoot`/KSUWebUI surface is rejected so the Android APK remains the sole UI. Release tags and module metadata use one canonical `vMAJOR.MINOR.versionCode` contract, and installed-state diagnostics require the entire module identity instead of trusting only `id`/`version`, so damaged metadata becomes repairable PARTIAL state. Module installation re-reads that identity under the update/lifecycle lock and rejects stale-dialog same/newer installs; same-version replacement is allowed only when the immutable checked release was explicitly marked as a missing/partial repair, a newer installed version is never downgraded, and identity loss after the check is rejected unless that immutable repair authorization was already present. First installation also refuses a pre-existing `modules_update` candidate and does not report the Magisk command as committed until one exact root-owned publication has the selected full module identity plus install-generation metadata bound to the downloaded archive SHA-256; verification remains cancellation-safe once the installer command starts. For a combined update, both downloads and both package preflights complete before the first module mutation; the APK is revalidated immediately before FileProvider handoff, cancellation after final download publication removes the completed cache file, and the next download prunes only canonical strictly named stale partials in the private update directory. A confirmed module commit is recorded before coroutine-context return and the remaining preflighted APK handoff runs cancellation-safely, so prompt cancellation cannot silently leave the pair half-finished. The local module builder now uses the same `MAJOR.MINOR.versionCode` contract as CI and rejects cross-series, zero, noncanonical, and out-of-range explicit versions instead of concatenating every digit. Duplicate/ambiguous release assets fail closed; invalid APKs are deleted before the private installer boundary is reachable.
- [completed] Keep update failures bounded and non-sensitive: remote HTTP bodies and raw SSL/JSON/PackageManager exception text are not surfaced, and every lifecycle argument in update/recovery commands is individually shell-quoted instead of appended as a raw command suffix.
- [completed] Minimize the APK installer boundary: cleartext app traffic is disabled, the non-exported `FileProvider` exposes only `cache/updates/` instead of the entire cache root, downloads reject unsafe/canonical-parent drift for final and partial paths, and the installer accepts only the canonical verified update APK name inside that directory. Update cache/staging directories are exact `0700`, downloaded and extracted regular files are exact `0600`, and the locally validated update guard is exact `0700`; modes are applied and re-read instead of inferred from umask. The unreachable pre-API-24 `file://` branch is removed.
- [completed] Align APK, installer, CI/package and device-preflight ABI policy to the exact primary Android ABI: only canonical `arm64-v8a`, `armeabi-v7a`, and legacy `armeabi` map to the two shipped ARM binary directories. Secondary translated ARM ABIs, x86/x86_64, wildcard vendor strings, and empty ABI lists fail before installation.
- [completed] Restore full runtime-manifest modes during Android hot update before publishing `candidate_ready`; all four root executables, every declared executable/data artifact and the generated installed binary now pass the same installed-mode plan as the standard installer. The Kotlin archive verifier and shell installer share an exact 37-entry non-negotiable runtime/provenance minimum, so a release manifest cannot redefine a self-consistent but non-installable/non-operational package by omitting lifecycle, recovery, configuration, strategy, Lua, wrapper, ABI, provenance, or installed-binary entries. ZIP creation is manifest-derived and both archive-name and extracted-tree checks reject undeclared entries outside `META-INF`; READY and first-install publication diagnostics cover the same critical regular/executable sets and primary ABI. Exact module identity, manifest-size, CRLF normalization, and generated-path rules are aligned across Kotlin and shell. READY diagnostics additionally require exact `0644`/`0755` critical modes, a safe module-root mode, and an exact empty root-owned `0600` disable marker.
- [completed] Remove the non-functional Wi-Fi SSID branch and its unnecessary `ACCESS_WIFI_STATE` permission: modern Android would require location-sensitive access the app neither requests nor needs, while authoritative network-type presentation remains available through `ACCESS_NETWORK_STATE`.
- [completed] Make Control polling consume one authoritative machine-status snapshot per refresh instead of issuing a second root status command through `NetworkStatsManager`; RUNNING now additionally requires Android's independent secure owner-file and exact iptables-topology verification, while a failed supplementary read degrades safely without losing stop capability.
- [completed] Bind Control polling to the destination's STARTED lifecycle instead of the ViewModel lifetime: background navigation cancels root polling, return performs an immediate refresh, initialization avoids a duplicate first probe, and an unexpected poll failure clears stale running/iptables success before the bounded retry interval.
- [completed] Bind Logs polling and protected read jobs to the destination's STARTED lifecycle: leaving the screen cancels polling plus command/output reads, restart events perform no background root I/O, returning refreshes the active tab, and an in-flight user-requested clear remains cancellation-safe.
- [completed] Replace lossy Control/Strategies snackbar `SharedFlow` channels with state-owned messages consumed through the shared lifecycle-aware snackbar effect; early initialization feedback and operation results that finish while the destination is stopped now remain visible on return instead of disappearing before subscription.
- [completed] Put Control diagnostics behind the same bounded privacy boundary as exported logs: dynamic and restored errors redact credentials, private identifiers, local addresses and app-private paths before display/persistence; unexpected update/lifecycle/archive exceptions use typed generic outcomes instead of raw exception messages.
- [completed] Bound protected log collection, rendering, and sharing; keep privileged I/O behind coroutine-aware repositories instead of UI code.
- [completed] Preserve user configuration across compatible upgrades and provide an explicit migration/recovery path for incompatible or corrupt state. Execution evidence remains tracked by the separate build/device gates.
- [completed] Make Strategies mutations field-scoped: category changes touch only one validated category plus `preset_mode`, packet/log changes never rewrite categories or switch modes, every rollback restores the exact prior state, and failed/loading state rejects both UI and programmatic mutations.
- [completed] Preserve the full canonical `pkt_out`/`pkt_in` range `1..999999999` across load, recreation and editing; remove the Control-side three-digit truncation and silent `1..100` clamping.
- [completed] Align Android runtime parsing with the exact shell section/key contract, including bounded canonical UID/GID values and exact `[core]` selection. Case/whitespace decoy sections can no longer poison reads or receive writes intended for the authoritative core.
- [completed] Require a successfully loaded or safely restored command-file binding before Config Editor permits edits, mode changes or saves; an initial read failure now leaves only retry enabled.
- [completed] Distinguish an ordinary rejected runtime write from an unverified rollback: Android re-reads the restored file, throws a dedicated fail-closed outcome when prior state is not recovered, and surfaces that outcome through Control, DNS, Presets, Strategies and Config Editor instead of reporting a routine save failure.

Acceptance criteria:

- [ ] App behavior is deterministic for every supported success/failure state and never reports running/applied/updated without post-operation verification.
- [completed] Static privileged-boundary audit confirms user-controlled names, routes, search text, file paths and lifecycle arguments are rejected by typed allowlists or individually shell-quoted before command construction; Android owner-state inspection additionally binds a secure root-owned state directory and stable regular-file identity across the read.
- [ ] Static analysis, lint, unit tests, and stress tests find no blocking main-thread I/O, lifecycle leak, unbounded retry, or concurrent-write race in covered flows.

### Phase 5 — Module and shell lifecycle hardening

- [completed] Add the fail-closed full-rollback transaction, exact machine contract, persistent disable fence, protected hosts backup, boot disable handling, and mandatory executable package/wrapper checks.

- [completed] Complete the Lifecycle Cleanup Audit implementation and split it into testable start, rollback, stop, boot, update, and uninstall operations; rooted-device evidence remains separate.
- [completed] Validate configuration and prerequisites before changing firewall/sysctl/process state; fail with a precise diagnostic and no partial activation.
- [completed] Add lifecycle locking and idempotency so app actions, service boot, action script, and manual shell invocations cannot race or create duplicate processes/rules.
- [completed] Make startup transactional: snapshot/track owned state, install only owned rules, verify nfqws2 health, and roll back all changes on any failure.
- [completed] Make stop/uninstall remove only module-owned processes/rules/files, including stale queue numbers and interrupted-start artifacts, while preserving unrelated firewall state.
- [completed] Restore every restorable sysctl, permission, ownership, routing, DNS, and firewall change owned by the module.
- [completed] Harden strategy/category/hostlist parsing against missing, empty, malformed, duplicate, incompatible, or partially updated files.
- [completed] Make category-mode construction fail closed: reject malformed/duplicate/unknown fields, unsafe or missing filter data, unknown protocols/strategies, and an all-disabled configuration instead of silently removing filters or substituting a global default; run the same machine validator before install/update publication and READY reporting.
- [completed] Validate all TCP/UDP/STUN strategy catalogs before normal construction, category validation, package publication and APK category presentation; require bounded regular non-symlink INI files, unique sections/keys, exactly one bounded non-empty `args` for usable strategies, and only the intentional empty `[disabled]` sentinel. Category input now uses the same 1 MiB/non-symlink boundary as Android.
- [completed] Add bounded log rotation and structured diagnostics covering versions, capabilities, selected config, process health, owned rules, and last failure without leaking private host data.
- [ ] Verify boot behavior for locked/unlocked devices, delayed data availability, no network, repeated service invocation, root-manager differences, and module-disable/remove flows.
- [ ] Verify supported IPv4/IPv6 and firewall backend behavior, queue-bypass semantics, kernel NFQUEUE availability, binary ABI/executability, SELinux denials, and clear unsupported-device failure.

Acceptance criteria:

- [ ] Start, stop, restart, boot, failed start, app update, module update, disable, reinstall, and uninstall are idempotent and leave no orphan nfqws2 process, stale NFQUEUE rule, stale lock, or unrestored owned setting.
- [ ] A failed or killed startup returns the device to the exact relevant pre-start state or a documented safe state and emits an actionable diagnostic.
- [completed] Direct shell regression sources cover quoting, safe paths with spaces, shell metacharacters/traversal, malformed runtime/owner/recovery state, missing or unsafe binaries/files/package entries, command/signal failures, exact rollback, and concurrent lifecycle/rule/lock ownership. The exact fixture map is retained in `docs/MD3E_EXPERIMENTAL_AND_TESTING.md`; Linux/root execution remains open below.

### Phase 6 — Automated test coverage and quality gates

- [completed] Add unit-test sources for reducers/view models, parsers, validators, migrations, repositories, update verification, command construction, and error mapping. Their synchronized-tree execution remains deferred.
- [ ] Add Compose UI tests for navigation, state rendering, semantics, destructive confirmations, persistence, repeated taps, back behavior, and core user journeys.
- [ ] Add screenshot/golden tests for all destinations and critical states across light/dark/dynamic themes, compact/expanded widths, and large fonts.
- [in progress] The isolated shell suite covers lifecycle rollback, persisted firewall generations, recovery classification, stale/foreign ownership, locking, config migration, diagnostics, signal durability, and package contracts. A pre-latest-edit Windows source snapshot passed `sh -n` for every shell file present at that time; this is historical evidence only. Syntax and behavioral execution must be rerun against the final synchronized tree under Linux root semantics, and rooted-device NFQUEUE verification remains pending.
- [in progress] Add device/emulator smoke tests for install/launch, root denied/missing module, status, configuration, and safe non-NFQUEUE flows. The fake-ADB host safety contracts and opt-in rooted-device harness are present; emulator/root-denied UI coverage and real-device execution evidence remain open.
- [ ] Configure CI to run formatting, static analysis, lint, unit tests, Compose tests, shell lint/tests, debug build, release build, and artifact checks from a clean checkout. The workflow now gates scoped full-tree whitespace/source hygiene, shell contracts, unit tests, an explicit debug APK, warning-as-error release lint, signed release assembly, signature/hash verification and module package contracts; a formal Kotlin formatter and the deferred Compose UI/golden matrix remain open.
- [completed] Fail CI on new warnings in owned Kotlin/lint/custom-Lua code, committed secret containers or PEM private keys, accidental release debug configuration, missing release metadata, and unsigned/unverifiable distributable artifacts; retain source-policy regressions for the fail-closed workflow contracts.

Acceptance criteria:

- [ ] A clean CI run produces reproducible APK/module artifacts and test reports with no skipped critical journey or unexplained flaky test.
- [ ] Every fixed logic defect has a regression test at the lowest practical layer.
- [completed] Source coverage is reported by destination/feature in `docs/MD3E_SOURCE_COMPLETION_AUDIT.md` and by state, concurrency, cancellation, malformed-protocol and shell-boundary risk in `docs/MD3E_EXPERIMENTAL_AND_TESTING.md`. Critical privileged/lifecycle paths map to direct unit and shell fixtures rather than a broad percentage; current-tree pass counts remain deferred.

### Phase 7 — Build, performance, security, and compatibility QA

- [completed] Enable release R8 code optimization and resource shrinking together, retain the optimized default rules plus native/coroutine safeguards, and make CI/source policy reject a release block that disables either optimizer. Remove the obsolete broad libsu package keep after verifying that the locked `core-5.2.2.aar` supplies its own `Shell.Initializer`/RootService consumer rules and release debug stripping. Actual mapping/resource/runtime verification remains part of the deferred release build gate.
- [ ] Run clean debug and release builds with the supported JDK/Gradle/AGP/Kotlin toolchain and record exact commands and versions.
- [ ] Verify release shrinking/obfuscation, resources, native ABI packaging, version metadata, signing expectations, install/upgrade/downgrade behavior, and module ZIP contents/permissions/line endings.
- [ ] Profile cold/warm startup, recomposition, list/log scrolling, memory, battery/wakelocks, shell latency, and APK/module size; set and meet explicit regression budgets.
- [completed] Confine all Android logging to one `BuildConfig.DEBUG`-guarded boundary so release builds emit no app/root/firewall stack traces or status chatter to logcat; add a repository-wide source policy against direct `Log`, `printStackTrace`, and stdout/stderr calls elsewhere.
- [completed] Remove the unowned eager root probe from `Application.onCreate`: startup now configures libsu only, while the screen/controller state owner performs the single authoritative root/module probe. This avoids a redundant concurrent root request whose result was discarded; a source policy prevents application-scope probing from returning.
- [in progress] Source security audit now locks the exact three-permission manifest, sole exported launcher, private update-only FileProvider, typed HTTPS About destinations, chooser-only bounded/redacted log export, cleartext denial, deny-all cloud/D2D backup rules, trusted GitHub/CDN redirects, exact `0600` APK handoff, protected path/quoting boundaries, and zero OSV matches across all 126 locked release-runtime components as queried on 2026-07-22. The full 585-component verification catalog still contains advisory matches confined to internal Unified Test Platform/build-host configurations plus stale unlocked metadata; upgrade/triage requires the deferred synchronized Gradle dependency resolve and remains open.
- [ ] Execute the supported device/root-manager/kernel/API test matrix, including at least one real rooted device for NFQUEUE behavior and lifecycle cleanup.
- [ ] Run soak and interruption tests: rapid toggles, rotation/backgrounding, process kill, reboot during operations, network loss, low storage, corrupt files, and update interruption.

Acceptance criteria:

- [ ] Release APK installs and upgrades cleanly, the module ZIP installs/updates/removes cleanly, and all core journeys pass on every required test-matrix tier.
- [ ] No critical/high security finding, crash, ANR, data-loss bug, orphan privileged state, or unexplained performance regression remains open.
- [ ] Unsupported environments fail safely with useful guidance and without modifying firewall or runtime state.

### Phase 8 — Visual and accessibility sign-off

- [ ] Capture before/after screenshots and final goldens for every destination and critical state using consistent device profiles.
- [ ] Review visual hierarchy, density, alignment, typography, color/state meaning, motion continuity, icon consistency, system bars, IME, and edge-to-edge behavior.
- [ ] Complete TalkBack traversal/action testing, contrast checks, touch-target checks, reduced-motion checks, switch-access/keyboard checks, and large-text/RTL review.
- [ ] Perform hands-on usability passes for first install, missing root/module, first successful start, strategy change, failure recovery, diagnostics export, and update.

Acceptance criteria:

- [ ] Final screenshots show a coherent Material 3 Expressive system with no clipping, overlap, illegible contrast, placeholder content, or legacy styling.
- [ ] All actionable controls have correct accessible names, roles, states, traversal order, and minimum target size.
- [ ] Motion communicates state without blocking interaction, and reduced-motion users retain equivalent feedback.

### Phase 9 — Release readiness and final completion audit

- [completed] Update user-facing documentation, permissions/root rationale, supported/unsupported matrix, migration/preservation notes, troubleshooting, and recovery/removal guidance in `docs/USER_OPERATIONS_RU.md` and `README.md`; both explicitly distinguish source readiness from pending signed-artifact/device evidence.
- [completed] Remove dead legacy UI/resources, obsolete direct dependencies, compatibility shims, debug hooks, temporary flags, stale UI TODO comments, and superseded UI paths after a source-usage audit. Intentional protocol recovery readers and platform theme compatibility remain explicitly scoped.
- [ ] Produce a signed release candidate and matching module ZIP; record hashes, version compatibility, build provenance, and rollback instructions.
- [ ] Re-run all automated gates and the full manual smoke matrix against the exact release artifacts, not only debug builds or source-tree scripts.
- [ ] Audit every checkbox and acceptance criterion in this roadmap against authoritative evidence; link the build/test reports, screenshots, device results, and known-limit decisions.

Final definition of done:

- [ ] The full app is Material 3 Expressive across every reachable state; no partial legacy UI remains.
- [ ] APK and module logic are internally consistent, transactional where privileged state changes, recoverable on failure, and verified on supported real-device configurations.
- [ ] All required automated checks and manual journeys pass on the final release artifacts with no critical/high defect or undocumented limitation.
- [ ] Existing Legacy Bootstrap Final Cleanup and Lifecycle Cleanup Audit items are completed with device evidence.
- [ ] The final acceptance audit finds no missing requirement, weak/indirect proof, or unverified critical path.
## Legacy UI final cleanup

- [completed] Reduce `AGENTS.md` to project architecture and coding guidance only; remove the entire agent/subagent, orchestration, delegation and parallel-work section plus the two obsolete untracked `.codex/agents` configurations (moved to the Windows Recycle Bin). Keep the remaining project guidance synchronized with the Compose/MD3E/navigation/ViewModel architecture and remove instructions prescribing XML layouts, Fragments, ViewPager, Fluent styling, or non-Compose UI.
- [completed] Remove the final production legacy UI paths and obsolete direct dependencies. The final source-only scan reports zero production/build references to platform Toast feedback, obsolete drawable/round-launcher names, removed helper/model/listener symbols, index compatibility shims, AppCompat/MDC/libsu-service, or Fluent/Win11 UI; obsolete layout/menu/navigation/drawable/color families are absent. Only the intentional platform window-theme resources for API 24 and the v26/v29/v31 light/night variants remain.
- [completed] Remove the duplicate illustrative preview UI. The 37 preview fixtures invoke all 10 real top-level destination entry points: 36 deterministic normal/loading/empty/error/disabled/degraded/transient states bypass Hilt and root/external work, while `About` remains stateless. Source configurations cover 360, 600, and 840 dp, light/dark, RTL, and Russian at 200% font scale.
- [completed] Remove the final stale app-state compatibility reads: Strategies no longer reads retired `pkt_count` as a live fallback, and the hostlist detail route no longer carries an ignored domain-count snapshot or a privileged root path. Navigation now carries only one validated filename; `HostlistRepository` derives the exact direct-child path internally, and entry counts come only from the protected file.
- [completed] Route every owned Compose tween duration through semantic MD3E motion tokens, including reduced-motion zero-duration behavior, and add a source policy against local numeric tween timings.
- [completed] Make the theme-owned animator-scale observer fully non-blocking and race-safe: initial and change-triggered `Settings.Global` reads run on `Dispatchers.IO`, a generation-keyed `LaunchedEffect` cancels stale reads, composition disposal unregisters the observer, and motion defaults to reduced until a finite authoritative scale arrives; read failures and non-finite values also remain fail-safe reduced. A source policy now locks lifecycle-aware collection for all nine stateful destinations and forbids unowned production coroutine scopes/observers.
- [completed] Remove the last direct preference read from the ViewModel main dispatcher: Control loads the persisted QUIC-banner dismissal on `Dispatchers.IO` before publishing initial UI state, while dismissal keeps Android's asynchronous `SharedPreferences.apply()` write. The privileged-boundary policy locks the IO-before-publication ordering.
- [completed] Make every full-screen text-input destination and the preset editor explicitly IME-aware under edge-to-edge, with bounded dialog editor height and a source policy preventing keyboard-overlap regressions.
- [completed] Add fail-closed `LegacyUiAbsencePolicyTest` coverage for forbidden source/dependencies/resources, platform-feedback regressions, theme boundaries, Snackbar behavior, and real-entry-point previews.
- [completed] Fix the `.gitignore` typo and add scoped ignores for Gradle/Kotlin output, root test temporary directories, local SDK properties, and signing secrets/artifacts without ignoring source fixtures.
- [completed] Make repository line endings deterministic across Windows/Linux: all text is normalized to LF, only `.bat` retains CRLF, and JAR/PNG assets are explicitly binary so packaged Android/Magisk shell contracts cannot inherit host-dependent CR bytes.
- [completed] After active test processes exited, enumerated and removed only workspace-contained generated `.test-tmp-*`, `.tmp-*`, and `android-app/.kotlin` directories: 36 validated non-reparse directories / 6,061,172 bytes moved to the Windows Recycle Bin, with zero matching directories left in the workspace.
- [ ] Re-run the synchronized strict/offline unit-test, compile, lint, and assemble graph after all cleanup inputs settle; require the final APK to postdate those inputs and record one authoritative suite/test count, lint result, APK size, and SHA-256.
- [completed] Run the final source-only zero-match audit. Production UI/build files contain no old View/XML/AppCompat/Fluent/Win11 implementation; test/docs references are policy assertions or historical context, and owner v3-v5 readers plus API-specific platform themes are the intentional compatibility allowlist.
- [completed] Remove the last misleading private Compose names containing `ListView`/`ViewContent`; strategy and hostlist production functions now use Compose-specific `CategoriesContent`/`HostlistBrowseContent`, so the legacy widget scan is literally zero without false-positive naming exceptions.
- [completed] Remove ten declaration-only shell helpers left behind by the owner-v6, teardown-journal and runtime-authority replacements; a repository-wide call-site scan now reports zero production shell functions that exist only as their own declaration, and the shell policy source forbids those retired symbols from returning.
- [completed] Remove the remaining test-only Kotlin surface from production: the `fitsUtf8EditorBudget`/`isValidPresetFileName`/`buildTerminalCommitCommand` adapters and three unused aggregate-budget constants. Tests now exercise the real normalized editor-budget, `PresetNamePolicy`, and terminal-plan APIs and derive cumulative limits from runtime constants plus the eight exact persisted keys; every declaration-only top-level function is an intentional `@Preview` entry point, with no declaration-only top-level property or internal member remaining.
- [completed] Close the return-during-operation lifecycle gap across Strategies, Presets, Hostlists, Hostlist Content, DNS, Config, and Hosts: every destination now binds entry/stop ownership to `LifecycleStartEffect`; a `STARTED` return overlapping load/save/import/restart/mutation queues one authoritative refresh, consumes it only after busy release, avoids self-cancelling the completing loader, and preserves dirty editor drafts above the refreshed baseline. Stop/disposal drops only the pending follow-up so it cannot perform a stale no-longer-visible read; the next start requests fresh truth. Lifecycle and privileged-boundary policies pin all nine screen bindings and all seven queued contracts; execution remains part of the deferred Gradle gate.
- [completed] Close cancellation/compensation holes in strategy, config, and preset restart paths: suspend category writes/validation and lifecycle restarts no longer sit inside `runCatching`, cancellation is rethrown, runtime rollback ambiguity remains `ROLLBACK_FAILED`, and a thrown preset restart now follows the same exact config/file compensation as a returned `false` for apply, save-and-apply, and category switching. Regression sources cover all three thrown-restart preset paths; execution remains deferred.
- [completed] Make hosts-overlay and hostlist conditional writes exception-safe after mutation: thrown write/remove or post-state reread failures now remain typed failures inside the repository, ensuring the captured overlay/file snapshot reaches the existing rollback path instead of escaping directly to an uncompensated outer catch. The source policy pins guarded post-read ordering before rollback.
- [completed] Close the shared `runtime.ini` exception gap at its owner: an exception from the primary atomic write or its post-read is now treated as ambiguous mutation, triggers restoration of the previous exact runtime content, and succeeds as rollback only after an independent secure reread; otherwise `RuntimeConfigRollbackException` remains visible to every mutation owner. The parity policy pins the write/read/rollback/reread ordering.
- [completed] Make app-private update cleanup subordinate to transaction ownership: the non-cancellable owner-lock release now runs before staging-tree cleanup and is protected by its own `finally`; partial/finalized downloads, prepared artifacts, extracted trees, and HTTP disconnects use isolated best-effort cleanup so one cache/connection failure cannot skip a sibling cleanup, mask the typed operation result, or orphan the privileged update owner. The source policy pins release-before-cleanup ordering; execution remains deferred.
- [completed] Restore cancellation after non-cancellable ownership release: the shared cross-process mutation lease now captures the block result, releases the exact lease, preserves primary/suppressed failure ordering, then checks caller cancellation before any normal return. Recovery-marker acquisition no longer has an early return that bypasses the post-release fence, and recovery also checks cancellation after its marker is retired. Direct regression source covers cancellation arriving while release is in progress; execution remains deferred.
- [completed] Canonicalize Hostlist Content process-death state: a restored search query is sanitized, bounded to 256 characters, and immediately rewritten into `SavedStateHandle` instead of leaving the oversized raw payload in the Activity Bundle. An incomplete editing/draft/baseline transaction is retired as one unit before viewer loading, matching the existing Config, Hosts, Presets, Control, and Strategies restoration policy. Direct durable-state sources cover both cases; execution remains deferred.
- [completed] Complete the Compose lazy-list source pass: all ten production `items`/`itemsIndexed` collections now declare an explicit stable key. High-volume hostlist/log rows reuse remembered number-format and monospace-style objects instead of allocating them inside every visible item composition; `ComposeLazyListPerformancePolicyTest` guards both invariants. Runtime scrolling/recomposition profiling remains part of the deferred performance gate.
- [completed] Finish the source-only Compose performance/conventions pass: all ten dynamic lazy collections now also declare an explicit reusable `contentType`; strategy reordering derives positions directly from `itemsIndexed` instead of performing a linear `indexOf` for every visible row; filtered hostlist/strategy results return to the first row when their query/mode identity changes; long-lived pagination, retry, completion and Snackbar effects read the current callback through `rememberUpdatedState`; editor/command text styles plus hostlist compact-number/file-size formatting are remembered by their exact inputs. The UI tree contains no direct shell/file/blocking I/O, all nine stateful destinations retain lifecycle-aware collection and STARTED ownership, and existing reduced-motion, touch-target, semantic-role, IME and adaptive-layout source policies remain intact. Compilation, lint, instrumentation and runtime profiling remain deliberately deferred by the current source-ready-only instruction.
- [completed] Harden the remaining UI-owned Android platform boundaries: hostlist detail navigation is single-top so rapid repeated taps cannot stack duplicate destinations, locale-sensitive compact counts observe `LocalConfiguration`, and Control clipboard failure/null-service cases now remain inside a guarded boundary with localized failure feedback instead of reporting a false success or crashing.
- [completed] Align hostlist filename/path admission with the case-sensitive shell catalog: APK creation, detail routing, record parsing, reads, searches, writes and rollback now require the exact lowercase `.txt` suffix used by the module's `*.txt` enumeration. Uppercase variants can no longer be written successfully and then disappear from the UI catalog; `HostlistRepositoryTest` pins filename, path and machine-record rejection.
- [completed] Align the remaining APK/shell filename boundary: Android simple-name validation now rejects the full ISO control range, including DEL (`U+007F`), matching shell `[[:cntrl:]]`; one parity policy locks the exact 16 release-owned command-line names across Android, runtime shell and package verification, plus the intentional lowercase-only preset/hostlist suffix and case-insensitive category dependency rules. Execution remains deferred.
- [completed] Enforce the actual 255-byte filesystem component limit across APK simple/runtime names, runtime/preset/dependency shell validators, and the standalone package verifier. Multibyte names can no longer pass character-count validation and fail later during protected file creation; direct Android and shell regression sources cover the exact 255-byte acceptance and over-limit rejection boundaries. Execution remains deferred.
- [completed] Remove the standalone package verifier's weaker `preset_file` validator: runtime selection and every preset manifest entry now use the same lowercase `.txt`, no-private-prefix, quote/separator/control, boundary-space and 255-byte contract as the APK/runtime command builder. A package can no longer validate successfully with a preset binding that the installed service must reject later. Execution remains deferred.
- [completed] Extend the same filesystem boundary to downloaded module archives: Android runtime-manifest parsing rejects preset entries through `PresetNamePolicy`, Android ZIP/manifests reject any path component over 255 UTF-8 bytes, and the shell package contract enforces the identical per-component byte limit. Regression sources cover a hidden preset manifest entry and an archive-only 256-byte component. Execution remains deferred.
- [completed] Reject file/child path-topology conflicts and collisions produced by ABI-template expansion across Android manifest/archive preflight and shell manifest/ZIP-name verification. Regression sources cover directory parents, file parents, duplicate file/directory names and an expanded ABI collision. Execution remains deferred.
- [completed] Fix the hosts editor's misleading exit-discard path: confirmation now clears the in-memory and SavedState draft before navigation, so returning to the destination cannot resurrect text the user explicitly discarded. A durable-state regression source locks the transition. Execution remains deferred.
- [completed] Finish the old-interface source purge with a complete production-tree guard instead of a curated file subset: every Kotlin source now rejects platform View/widget, Fragment/AppCompat/RecyclerView/ViewBinding/DataBinding/MDC/XML-layout and removed compatibility symbols, while direct legacy UI dependencies and View resource families remain forbidden. Remove the final orphaned old preset-card action string from both locales; all remaining strings/plurals are referenced. The only retained XML colors/themes are the platform startup, splash, and system-bar contract; transaction/owner `legacy` readers are non-UI recovery compatibility and remain intentionally intact. Execution remains deferred.
- [completed] Remove the last visible unsupported-setting placeholder from the Compose interface: Control no longer renders a permanently disabled Wi-Fi-only switch with an empty callback. The runtime still normalizes legacy `wifi_only=1` safely and retains its localized migration-failure message, but the finished UI exposes only settings the current module can actually mutate. Execution remains deferred.
- [completed] Make editor navigation complete by construction: Config and Hosts no longer provide a silent default no-op back callback. Every runtime route must supply deterministic navigation, while preview-only call sites declare their inert callback explicitly. Execution remains deferred.

Evidence status: documentation, project guidance, ignore rules, production/resource/dependency cleanup, generated-directory cleanup, the fail-closed absence policy, and real-entry-point preview sources are complete by static source audit. Per user direction, compile/lint/test/APK gates remain explicitly pending; the new policy has not been executed after the latest source-only edits, and earlier APK/test numbers below are historical evidence rather than current-tree proof.

## Theme and Resource Final Audit

- [completed] Fix API 26–28 light-navigation-bar contrast while retaining the API 24–25 dark fallback and API 29+ transparent behavior.
- [completed] Remove the 71 zero-consumer Kotlin color compatibility aliases and 44 proven-orphan XML drawables.
- [completed] Remove the remaining legacy Fluent palette and obsolete layout/menu/navigation/drawable/color resource families; retain only the intentional platform window-theme resources for API 24 plus v26/v29/v31 light/night behavior.
- [completed] Re-run static resource reachability after cleanup: all eight file-backed launcher/provider/backup-policy resources and all 510 default string/plural IDs have production consumers; all 510 unique Kotlin `R.*` references and 22 manifest/resource XML references resolve against the post-legacy tree, all 18 manifest/resource XML files parse, and no legacy file-resource family remains. `ResourceLinkagePolicyTest` locks the cross-reference graph.
- [completed] Add shared spacing, elevation, component-size, breakpoint, editor/sheet-size, and motion tokens with an observable reduced-motion policy; one theme-owned system animator observer publishes the preference through a CompositionLocal consumed by shared cards, rows, overlays, dialogs, pickers, indicators, screens, and preview layouts.
- [completed] Replace all literal dp values in the adaptive `MainActivity.kt` shell and 10 destination `*Screen.kt` files with role-named tokens; enforce the empty exception allowlist with `ScreenDesignTokenPolicyTest` and `docs/MD3E_DESIGN_TOKENS.md`.
- [completed] Normalize the core default catalog to English and add a parity-complete Russian catalog for the same resource IDs.
- [completed] Add the 15 durable config/hosts result messages to both locale catalogs and replace all 13 quantity-bearing IDs with locale-correct English/Russian plurals.
- [completed] Replace static user-facing Compose copy and accessibility descriptions with parity-complete default-English and Russian resources; keep only protocol/state IDs and tooling labels inline, and mark visible immutable identifiers and paths non-translatable.
- [completed] Replace the full-screen illustrative duplicate with 37 preview-safe fixtures invoking the real top-level entry points for all 10 destinations. The nine stateful screens use 36 deterministic normal and non-happy `previewState` scenarios, `About` is stateless, preview guards suppress external/root work, and source fixtures cover 360/600/840 dp, light/dark, RTL, and Russian 200% font configurations.
- [completed] Document the current test-runtime boundary in `docs/MD3E_EXPERIMENTAL_AND_TESTING.md`: JVM policy tests are available, while canonical Maven Central TLS blocked a strictly verified Compose UI/Robolectric/Roborazzi graph; no regional mirror or provisional baseline was retained.
- [completed] Historical pre-cleanup baseline only: the locked graph previously passed strict/offline dependency resolution, unit tests, debug/release compile and lint, and debug APK assembly. It must not be used as current-tree completion evidence after the legacy cleanup changes.
- [completed] Update the README for the adaptive Jetpack Compose Material 3 Expressive shell and all 10 destinations, including the hostlist content/details route.
- [in progress] Final synchronized-tree strict Android gate must be rerun after legacy cleanup. Do not publish a replacement test count, lint result, APK size, timestamp, or SHA-256 until one stable-tree run supplies all of them.
- [in progress] External final gates: run the skipped symlink contract and shell suite on Linux CI, produce and verify the signed release artifacts, and complete rooted-device/Magisk/firewall/accessibility validation.

### Separate out-of-scope UI roadmap

- [completed] Add bounded Logs sharing through the Android Sharesheet: only the most recent 64 KiB is exported as text, with explicit empty/failure feedback and no provider or storage permission.
- [completed] Add Hostlists import/update through `OpenDocument` with a 1 MiB limit, strict UTF-8 and safe-filename checks, domain/IP/CIDR validation, serialized atomic root writes, and durable imported/updated/error feedback.
- [completed] Give hostlist catalog totals, detail pagination, search, and post-save state one data-entry definition: trim entries and exclude blank/comment lines everywhere. Validate editor content before mutation and again at the root repository boundary, while retaining the internal wildcard masks used by packaged lists.
- [completed] Close the remaining transient/terminal state gaps: remove the double release of the update-check gate, clear DNS selections after reset, invalidate DNS after rollback failure, reconcile or fail-close Control runtime settings after uncertain rollback, and preserve distinct lifecycle-blocked outcomes for log clearing and strategy-order saving.
- [completed] Add a durable Control `lastResult` presentation backed by `SavedStateHandle`, independent from transient rollback/dialog cleanup.
- [completed] Complete the source-only privileged I/O boundary: Compose performs no repository/root work; ViewModels construct no shell commands or protected paths; hostlist imports, protected logs, command lines, hosts overlays, diagnostics, categories, strategies, and presets use bounded typed data boundaries.
- [completed] Complete source-only atomic-write parity for editable app flows: hostlists, runtime configuration, DNS/hosts overlays, command lines, categories, and presets snapshot before mutation, serialize through the module mutation lease, restore on ambiguous failure, and expose blocked/rollback-failed outcomes instead of reporting stale success.
- [completed] Remove duplicate strategy selection state and move picker loading/order/retry into `StrategiesViewModel`; strict category/strategy parsers reject missing required fields, duplicate or unknown sections/keys, unsafe active/alternate bindings, and malformed values.
- [completed] Close the final strategy-order and cross-resource compensation gaps: order saves persist and write one immutable current snapshot, while Config Editor and Presets restore their already-written companion file when a `runtime.ini` commit becomes ambiguous.
- [completed] Make optional `runtime.ini` sections fail closed on duplicate exact sections/keys, validate every `[core]` scalar including forward-compatible keys, and align the real shell strategy reader with Android for whitespace-tolerant `args = ...`, bounded size, root ownership, safe mode, and single-link files.
- [completed] Bind Config Editor mutations to one verified authoritative command file and show an explicit unavailable binding instead of a fictitious fallback path after load failure.
- [completed] Make Hostlists, Hosts Editor, Hostlist Content, DNS, Presets, and Logs fail closed after protected-read failures: stale catalog/content is cleared or made read-only, mutations require an authoritative current baseline, and retry remains available.
- [completed] Route thrown Logs repository failures through the same typed error states as explicit protected-read failures, so command loading cannot remain stuck and polling cannot die while stale output remains actionable.
- [completed] Add optimistic source-change protection to hosts and hostlist editors: restored drafts are revalidated before editing, concurrent source changes block writes, exact post-write content is verified, and drafts survive recovery.
- [completed] Preserve the exact post-write hostlist bytes as the next editor baseline, eliminating false second-save `SOURCE_CHANGED` results caused by newline normalization. Bind editor entry and search/page reads to generation tokens and immutable queries so an older root read cannot publish into newer viewer/editor state.
- [completed] Revalidate authoritative module state when saved Compose destinations re-enter composition: Strategies, Presets, Hostlists, DNS, Config, Hosts, and Hostlist Content refresh after back-stack return without duplicating their initial read; dirty editor drafts remain preserved over the newly verified baseline, and delayed hostlist searches are cancelled before the return refresh.
- [completed] Extend authoritative-baseline and optimistic source-change protection to the preset editor, including restored-draft revalidation, exact post-replace verification, recoverable close controls when catalog reload fails, and a durable source-changed outcome.
- [completed] Extend the same protection to Config Editor: restored drafts are read-only until runtime/manual binding revalidation, saves compare the exact configured manual snapshot and editor baseline, external changes return a durable conflict outcome, and successful writes are canonically re-read before becoming the new baseline.
- [completed] Add an exact versioned cmdline machine-validator shared with the runtime builder; Config Editor now validates the temporarily written command through the module and restores the previous file before reporting any rejected or unverifiable save.
- [completed] Remove Config Editor's misleading runtime-command fallback: the editor now binds only to the configured manual file (missing/blank is shown honestly as empty), while the separately bounded Logs destination remains the sole runtime-command viewer.
- [completed] Make the protected command-file reader actually honor the blank-file contract through an explicit secure zero-byte opt-in, while the shared protected reader remains non-empty by default.
- [completed] Honor the Hostlist empty-state contract in edit mode: securely verified zero-byte lists can now be snapshotted and edited instead of turning the documented empty state into a false protected-read failure.
- [completed] Align runtime custom-command validation with the APK and package contract: consistently accept protected `0600/0644` inputs, and reject files over 256 KiB, non-root ownership, unsafe modes, hard links, metadata/content changes during parsing, and unsafe referenced Lua/blob/hostlist/ipset files before any command can be accepted.
- [completed] Carry the same custom-command mode contract through standard-install and hot-update preservation, rejecting unsafe source modes and re-verifying the normalized root-owned `0644` target instead of silently legitimizing a world-writable input.
- [completed] Make Config Editor validation prove executable validity, not just allowlist membership: the read-only machine path loads the exact current core config, builds the complete cmdline without state writes, verifies the installed root-owned `0755` binary, and requires its real `--dry-run` to succeed before emitting `OK`.
- [completed] Keep cmdline validation failures truthful: only a parser or nfqws2 dry-run rejection emits exact `INVALID`; unavailable runtime, binary, or full-build infrastructure emits a distinct machine error so APK does not blame and label the user's command incorrectly.
- [completed] Make manual file references deterministic across APK validation and runtime start: empty/traversing paths fail closed, safe relative Lua/blob/hostlist/ipset paths are normalized against the exact module root, and the resulting absolute protected file is verified before use.
- [completed] Add source coverage for the complete cmdline machine protocol using a disposable root-owned module fixture and dry-run binary: exact `OK`/exit 0, user `INVALID`/exit 1, and binary `ERROR`/exit 2 records are independently asserted.
- [completed] Confine every manual file-bearing option to its exact protected module root and a direct child (`lua`, `bin`, or `lists`), preserving approved user extensions while rejecting arbitrary absolute external paths, nested escapes, wrong file classes, and replaceable directory inputs.
- [completed] Keep manual-command validation attribution exact when integrity tooling is unavailable: the machine protocol now emits a distinct infrastructure error instead of mislabeling the user's command as invalid, and README documents the complete bounded manual-mode contract.
- [completed] Keep every command-builder machine interface protocol-specific at the trust boundary: unsafe root arguments now return the matching preset/category/strategy/cmdline error family instead of a misleading preset record.
- [completed] Reject oversized Config, hosts-overlay, hostlist-editor, preset-editor, and generated DNS/hosts content before the first write, so a validation rejection cannot trigger a fictitious rollback attempt or rollback-failed warning.
- [completed] Replace four independent 64-Ki-character editor `SavedStateHandle` limits with one shared 16-Ki-character field budget, keeping the eight simultaneous draft/baseline fields at or below 128 Ki characters while leaving live editor and protected-file limits unchanged.
- [completed] Bound strategy-order process recovery cumulatively: at most four category arrays and 16 Ki identifier characters each, with immediate stale-key cleanup on restore so obsolete arrays cannot silently remain in the Activity Bundle.
- [completed] Reject oversized Config, hosts, hostlist and preset text before it enters long-lived Compose state, using one allocation-free UTF-8 counter aligned with repository byte limits while retaining the independent pre-write validation boundary.
- [completed] Cache editor line-count derivation by exact text identity in Config, Hosts and Hostlist Content; large drafts are no longer rescanned during unrelated MD3E recompositions, Config no longer performs the same full pass twice per supporting-label render, and the strategy picker no longer copies/filters its catalog unless its query, mode, or ordered list actually changes.
- [completed] Bound hostlist, log and strategy-picker query text before retaining it in Compose or process state; cancel and invalidate stale strategy-picker loads whenever configuration reload or mutation begins.
- [completed] Make editor size accounting match exact persisted bytes: Config includes the mandatory final newline and Preset validates its LF-normalized candidate instead of rejecting or accepting based on a different CRLF representation.
- [completed] Reject protected editor baselines that fit only in their raw representation but cannot be canonically rewritten within the same byte limit; command, hosts, hostlist and preset snapshots are now rollback-safe at the boundary.
- [completed] Strengthen the shared protected-text reader, Android owner-v6/firewall identity re-read, and update transaction/cleanup journal readers with byte-level SHA-256 stability before/after content consumption; protected text also rejects NUL. An in-place concurrent rewrite that preserves inode, owner, mode, link count and size can no longer be accepted as an authoritative APK snapshot or recovery decision input.
- [completed] Replace the three duplicated APK bounded-stream loops with one overflow-safe reader shared by release metadata, module-package verification, and document-provider hostlist import. Every remaining download, extraction, and digest loop uses the same guaranteed-progress primitive, so a zero-progress bulk read falls back to a single-byte read instead of spinning forever; exact-limit/over-limit/zero-progress regression sources lock the boundary.
- [completed] Make the download producer honor its documented `0..100` callback contract for untrusted HTTP `Content-Length`; undersized metadata is capped, unknown/invalid/over-budget lengths produce no percentage, and the existing consumer-side normalization remains an independent UI boundary.
- [completed] Complete the reviewed artifact redirect status allowlist for GET downloads: `301`, `302`, `303`, `307`, and `308` are followed only after resolving and revalidating the next HTTPS GitHub/release-CDN URL; all other statuses fail closed.
- [completed] Make hostlist search count and page one bounded single-pass snapshot operation; the UI no longer combines a count from one file version with rows from a second read, large-list search avoids duplicate full-file I/O, and whitespace-only queries consistently return to the unfiltered list instead of producing a misleading search state.
- [completed] Close the final source-only navigation/destructive-action gaps: double-back exit uses monotonic elapsed time, rejects reversed timestamps, and resets its confirmation window on route/drawer changes; DNS reset now requires an explicit localized confirmation before mutating runtime/hosts state.
- [completed] Make process-state restoration fail closed: typed enum/schema restoration removes stale or mistyped Bundle payloads, invalid hostlist routes clear their route-scoped drafts/results, and incomplete Control packet/error/rollback payloads collapse to no dialog instead of leaving an invisible modal state that blocks actions. Restored diagnostics are redacted and rewritten canonically.
- [completed] Bind every saveable destructive/editor dialog to its current authoritative baseline and busy state so restored confirmations cannot reappear after reload, save, reset, clear, or catalog loss. Strategy detail selection now clears when its category authority is invalidated, and parentless restored detail/editor exits replace the orphan destination with the deterministic Hostlists/Control root. Recovery/update lifecycle trust now includes the exact sourced `common.sh` and `command-builder.sh` files, not only the directly invoked start/stop/status scripts.
- [completed] Extend the localized-copy boundary through ViewModel-produced `UiText`: app-owned literal `UiText.Dynamic` values are rejected while exact runtime/user/diagnostic values remain typed dynamic data. Config and Strategies runtime-mutation adapters now rethrow `CancellationException` before mapping ordinary exceptions to failure, and a lifecycle source policy locks both adapters.
- [completed] Replace English exception strings in update-check UI with a typed failure protocol covering offline, timeout, TLS, HTTP, oversized/empty/invalid metadata, and generic request failures. Control maps every reason to an English/Russian resource; the data layer no longer supplies user-facing prose for this flow.
- [completed] Remove the English `No changelog available` sentinel from release parsing. Missing or blank release notes remain empty runtime data and the MD3E update dialog supplies the localized English/Russian fallback resource.
- [completed] Replace download/preflight/install prose in `UpdateExecutionReport` with typed download, validation, unsupported-ABI, installer, recovery, rejection, installation, and deferred reasons. Terminal outcomes carry the exact typed failure instead of English `Module:`/`APK:` concatenation; Control localizes app-owned reasons and recursively composes localized partial-update context. APK and module preflight now expose 18 typed validation reasons with exact English/Russian resources instead of surfacing validator-authored English prose; only bounded redacted recovery diagnostics remain dynamic.
- [completed] Audit every ViewModel/Compose exception and diagnostic boundary after the update refactor. Non-Control screens retain only resource-backed `UiText` or exact trusted runtime/user data; Control is the sole technical-diagnostic presenter and sanitizes/redacts it before dialog state or persistence. Regression sources require exhaustive download/validation enum coverage and reject a return to validator-authored APK/module/rejection prose.
- [completed] Wrap fatal module-update recovery diagnostics in localized Control context without weakening the persisted error-resource allowlist. The wrapper resource ID and its separately bounded/redacted argument are restored only as one approved pair; missing arguments fall back to generic recovery guidance, unknown IDs cannot promote a stale dynamic payload, and canonical reconstruction rewrites the safe pair after process death.
- [completed] Centralize cancellation safety for admitted multi-step APK module mutations. Config, DNS, hostlist import/content, hosts overlay, Strategies and Presets now enter one coordinator-owned `withNonCancellableMutation` boundary, so screen cancellation cannot release the lifecycle lease between snapshot, write, verification and rollback; ordinary suspend adapters still rethrow cancellation before mapping failures. A source policy rejects restoring scattered ViewModel-owned transaction fences.
- [completed] Close the Hostlist Content debounce/paging race: a new query immediately invalidates old entries, counters, page index and READY state before the debounce suspension, so lazy-list paging cannot combine the new query with the previous page cursor. Invalid route instances also reject search at the ViewModel boundary.
- [completed] Route the delayed post-update Control refresh through the cancellation-aware status adapter instead of a raw `checkStatus()` call. Ordinary root/status failures now produce the deterministic unavailable state rather than escaping uncaught from `viewModelScope`, and leaving the screen suppresses the delayed background root read until lifecycle re-entry.
- [completed] Stop Hostlist Content pagination at the authoritative file/search count. The lazy-list trigger and ViewModel now share `canLoadMore`, preventing a guaranteed extra privileged page read after the last viewer or filtered row.
- [completed] Bind Logs sharing to the current authoritative tab and exact ready payload at the ViewModel boundary, matching clipboard safety. A stale Compose click after tab/load/clear/lifecycle change is rejected without opening the chooser; empty current output and platform share failure remain distinct localized outcomes.
- [completed] Bind Logs destructive confirmation to the exact expected tab. The ViewModel verifies it before and after claiming the clear gate, while Compose invalidates a saved confirmation when the tab changes, so a stale Logs confirmation cannot clear Warnings or vice versa.
- [completed] Bind remaining identity-sensitive Compose state: Strategy Detail now resynchronizes its local filter selector whenever the authoritative category filter changes after reload, optimistic commit, or rollback; Preset Editor discard confirmation is saveable only for the exact `fileName` and cannot carry into another editor instance.
- [completed] Finish the saveable-state audit: Logs clear confirmation is keyed directly to the authoritative tab, and strategy reordering cannot open, save, or remain active while its exact picker catalog is loading, failed, or empty.
- [completed] Bind strategy-order persistence to the exact authoritative picker snapshot: stale callbacks, changed catalog membership, changed runtime order, duplicate/unknown IDs, and an external runtime-order write before the atomic compare-and-set are rejected without replacement.
- [completed] Remove the last caller-owned cancellable multi-step mutation fence: Control packet-count changes now keep the lifecycle lease across runtime write, authoritative UI update, conditional restart, and post-restart verification; direct `withMutation` remains restricted to genuinely atomic repository/log operations.
- [completed] Move Control's synchronous protected failure-log tail read off the main dispatcher. Failed start/stop diagnostics can no longer block MD3E rendering while root validates and reads the private log files.
- [completed] Bind DNS apply to the exact current packaged `hosts.ini` catalog under the lifecycle lease. A ViewModel retained across hot update now rejects its stale preset/service snapshot before touching overlay/runtime state, clears the obsolete selection, reloads the new catalog, and explains the required review in localized UI.
- [completed] Make `hosts.ini` parsing fail closed on missing/reordered/duplicate sections, orphan or empty services, duplicate labels/domains/direct entries, malformed hostnames/IPs, inconsistent address cardinality, unknown selections, and invalid preset indexes. Repair the packaged Deezer block separator and remove eight duplicate direct mappings; a repository-source contract pins the resulting 7 presets, 57 DNS services, 11 direct services, 803 domains, and uniform six-address mapping.
- [completed] Reject malformed manual hosts overlays before the mutation lease or first write: active lines require a numeric IPv4/IPv6 address and one or more bounded ASCII hostnames, while blank lines and full/inline comments remain supported. DNS generation shares the same address/hostname validator, validation feedback is localized and previewed, and rollback deliberately bypasses the new syntax gate so it can restore an exact pre-existing bounded snapshot.
- [completed] Close the hosts-overlay source-check TOCTOU at the repository boundary: editor and DNS writes/removals re-read the exact overlay plus effective hosts content immediately before mutation, reject drift as a distinct localized source-changed outcome, and verify the exact persisted overlay or confirmed missing-overlay post-state before success. Reset can no longer mistake a concurrently appeared overlay for a successful removal.
- [completed] Make managed DNS marker parsing line-exact: merge/remove accept canonical LF or CRLF marker lines only, reject embedded, suffixed, missing, duplicate, or reversed marker tokens, and can no longer cut an arbitrary hosts line at a marker substring.
- [completed] Make ordinary hot-update failure compensation cancellation-safe too: once a transaction exists or the service has been stopped, rollback/restore now completes under `NonCancellable`, records whether recovery artifacts must remain, and only then restores caller cancellation. `PrivilegedProtocolParityPolicyTest` locks both compensation fences and the post-compensation cancellation exit; `ComposeLifecyclePolicyTest` rejects `runCatching` from every production ViewModel so suspend cancellation cannot silently become an ordinary UI failure.

### Real-device visual and accessibility gate — not yet completed

Automated source, resource, lint, and build checks do not replace the following hands-on checks on physical devices. Keep each item open until results and screenshots are recorded for the final build.

- [ ] Verify API 24, API 26, API 27, API 28, and the current Android/API release on physical devices, including the API-specific navigation-bar behavior.
- [ ] Verify light and dark themes on every applicable API tier, plus dynamic color on a supported current-Android device.
- [ ] Verify compact phone, 600 dp, and 840 dp window widths, including landscape at each supported width class.
- [ ] Verify English, Russian, and an RTL locale at 100% and 200% font scale; include long labels, truncation, wrapping, and editor IME behavior.
- [ ] Verify TalkBack traversal order, names, roles, states, actions, focus restoration, and all minimum touch targets by hand.
- [ ] Verify reduced motion with system animator scale set to 0× and compare it with normal motion while preserving equivalent state feedback.
- [ ] Verify status and navigation bars, icon contrast, edge-to-edge insets, gesture navigation, keyboard insets, and display cutouts in every applicable theme/API combination.
